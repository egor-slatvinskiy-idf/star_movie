import 'package:flutter/material.dart';

class MovieComingSoonWidget extends StatelessWidget {
  const MovieComingSoonWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
