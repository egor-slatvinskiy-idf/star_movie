class MovieListModel {
  final int id;
  final String title;
  final String description;

  const MovieListModel({
    required this.id,
    required this.title,
    required this.description,
  });
}